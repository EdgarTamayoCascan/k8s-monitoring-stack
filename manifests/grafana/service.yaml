apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: monitoring
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/component: visualization
spec:
  type: NodePort
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
      nodePort: 30300
  selector:
    app.kubernetes.io/name: grafana
